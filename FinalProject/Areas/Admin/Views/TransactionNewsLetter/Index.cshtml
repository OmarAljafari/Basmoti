@model IEnumerable<FinalProject.Models.TransactionNewsletter>

@{
    ViewData["Title"] = "Index";
}

<section class="section col-lg-12">

    <div class="row">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-body">
<table class="table table-responsive">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TransactionNewsletterEmail)
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionNewsletterEmail)
            </td>
           
        </tr>
}
    </tbody>
</table>

</div>
</div>
</div>
</div>
</section>